# StayOnline Wire blog

Jekyll source for the StayOnline Wire blog.

## Local setup

After cloning, point git at the repo's tracked hooks so tag pages and the tag
graph get regenerated automatically before each commit:

```sh
git config core.hooksPath .githooks
```
